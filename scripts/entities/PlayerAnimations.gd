# Directions.gd
extends AnimationTree

# En tu script principal (junto con el resto de tu código)

const RIGHT = Vector2(1, 0)
const LEFT = Vector2(-1, 0)
const DOWN = Vector2(0, 1)
const UP = Vector2(0, -1)
